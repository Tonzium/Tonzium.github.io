window.mermaidConfig = {
    startOnLoad: true,
    theme: 'dark',
    securityLevel: 'loose',
    quadrantChart: {
        width: 400,
        height: 400
    }
};

// Initialize mermaid
mermaid.initialize(window.mermaidConfig);
